An experiment at writing a minitest-like framework for Crystal using
structs and that does zero allocations, so it can used without a GC or
the official stdlib.

Note: it has since been merged into [nanolib](https://github.com/ysbaddaden/nanolib.cr).
